@model FootBallOne.Models.CampViewModel
@{
    ViewData["Title"] = "Edit Camp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Camp</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #f8fafc;
            color: #334155;
        }

        .header {
            background: white;
            padding: 20px 40px;
            border-bottom: 1px solid #e2e8f0;
        }

        .breadcrumb {
            color: #64748b;
            font-size: 14px;
            margin-bottom: 8px;
        }

            .breadcrumb a {
                color: #64748b;
                text-decoration: none;
            }

                .breadcrumb a:hover {
                    color: #0891b2;
                }

        .page-title {
            font-size: 24px;
            font-weight: 700;
            color: #1e293b;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            min-height: calc(100vh - 120px);
        }

        .progress-container {
            padding: 40px 80px 30px;
            background: white;
        }

        .progress-steps {
            display: flex;
            justify-content: space-between;
            position: relative;
            max-width: 800px;
            margin: 0 auto;
        }

            .progress-steps::before {
                content: '';
                position: absolute;
                top: 20px;
                left: 20px;
                right: 20px;
                height: 2px;
                background: #e2e8f0;
                z-index: 1;
            }

        .progress-line {
            position: absolute;
            top: 20px;
            left: 20px;
            height: 2px;
            background: #0891b2;
            z-index: 2;
            transition: width 0.3s ease;
            width: 0%;
        }

        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            z-index: 3;
            flex: 1;
            cursor: pointer;
        }

        .step-circle {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 14px;
            border: 2px solid #e2e8f0;
            background: white;
            transition: all 0.3s ease;
            color: #64748b;
        }

        .step.active .step-circle {
            background: #0891b2;
            color: white;
            border-color: #0891b2;
        }

        .step.completed .step-circle {
            background: #0891b2;
            color: white;
            border-color: #0891b2;
        }

        .step-label {
            margin-top: 12px;
            font-size: 14px;
            font-weight: 500;
            color: #64748b;
            text-align: center;
        }

        .step.active .step-label {
            color: #0891b2;
            font-weight: 600;
        }

        .step.completed .step-label {
            color: #0891b2;
        }

        .form-content {
            padding: 40px 80px 60px;
            max-width: 800px;
            margin: 0 auto;
        }

        .step-content {
            display: none;
        }

            .step-content.active {
                display: block;
            }

        .form-group {
            margin-bottom: 32px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #374151;
            font-size: 14px;
        }

        .required {
            color: #ef4444;
        }

        input[type="text"],
        input[type="number"],
        input[type="date"],
        textarea,
        select {
            width: 100%;
            padding: 12px 16px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            font-size: 14px;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
            background: white;
            font-family: inherit;
        }

            input:focus,
            textarea:focus,
            select:focus {
                outline: none;
                border-color: #0891b2;
                box-shadow: 0 0 0 3px rgb(8 145 178 / 0.1);
            }

            input.error,
            textarea.error,
            select.error {
                border-color: #ef4444 !important;
                box-shadow: 0 0 0 3px rgb(239 68 68 / 0.1) !important;
            }

        textarea {
            resize: vertical;
            min-height: 120px;
        }

        select {
            appearance: none;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
            background-position: right 12px center;
            background-repeat: no-repeat;
            background-size: 16px;
            padding-right: 40px;
        }

        /* Toggle Switch Styles */
        .toggle-container {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .toggle-switch {
            position: relative;
            width: 52px;
            height: 28px;
            display: inline-block;
        }

            .toggle-switch input {
                opacity: 0;
                width: 0;
                height: 0;
            }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #cbd5e1;
            transition: 0.3s;
            border-radius: 28px;
        }

            .toggle-slider:before {
                position: absolute;
                content: "";
                height: 20px;
                width: 20px;
                left: 4px;
                bottom: 4px;
                background-color: white;
                transition: 0.3s;
                border-radius: 50%;
            }

        input:checked + .toggle-slider {
            background-color: #10b981;
        }

            input:checked + .toggle-slider:before {
                transform: translateX(24px);
            }

        .toggle-label {
            font-weight: 500;
            color: #374151;
            font-size: 14px;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 500;
        }

        .status-active {
            background-color: #d1fae5;
            color: #065f46;
        }

        .status-inactive {
            background-color: #fee2e2;
            color: #991b1b;
        }

        .activities-section h3,
        .facilities-section h3 {
            margin-bottom: 24px;
            color: #374151;
            font-size: 16px;
            font-weight: 500;
        }

        .activity-item,
        .facility-item {
            display: grid;
            grid-template-columns: 1fr 2fr auto;
            gap: 20px;
            align-items: start;
            margin-bottom: 20px;
            padding: 20px;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            background: #f8fafc;
        }

        .delete-btn {
            background: transparent;
            color: #ef4444;
            border: none;
            width: 32px;
            height: 32px;
            border-radius: 6px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            transition: background-color 0.2s ease;
            margin-top: 32px;
        }

            .delete-btn:hover {
                background: #fef2f2;
            }

        .copy-icon {
            color: #6b7280;
            font-size: 16px;
            cursor: pointer;
            margin-top: 32px;
            margin-right: 8px;
        }

            .copy-icon:hover {
                color: #374151;
            }

        .add-btn {
            background: #f8fafc;
            color: #64748b;
            border: 1px solid #e2e8f0;
            padding: 12px 24px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            font-size: 14px;
            transition: all 0.2s ease;
            margin-top: 16px;
            display: inline-block;
        }

            .add-btn:hover {
                background: #f1f5f9;
                color: #475569;
            }

        .map-container {
            width: 100%;
            height: 300px;
            border: 1px solid #d1d5db;
            border-radius: 8px;
            margin-bottom: 20px;
            position: relative;
            overflow: hidden;
        }

        .form-navigation {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 30px 80px 40px;
            background: white;
            border-top: 1px solid #e2e8f0;
        }

        .nav-btn {
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            font-size: 14px;
        }

        .btn-back {
            background: white;
            color: #64748b;
            border: 1px solid #e2e8f0;
        }

            .btn-back:hover:not(:disabled) {
                background: #f8fafc;
                color: #475569;
            }

        .btn-next {
            background: #0891b2;
            color: white;
            min-width: 80px;
        }

            .btn-next:hover:not(:disabled) {
                background: #0e7490;
            }

        .btn-save-changes {
            background: #10b981;
            color: white;
            min-width: 120px;
        }

            .btn-save-changes:hover:not(:disabled) {
                background: #059669;
            }

        .nav-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .action-buttons {
            display: flex;
            gap: 16px;
        }

        .btn-cancel {
            background: white;
            color: #64748b;
            border: 1px solid #e2e8f0;
        }

            .btn-cancel:hover {
                background: #f8fafc;
            }

        .success-message {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            display: none;
        }

        .error-message {
            color: #ef4444;
            font-size: 12px;
            margin-top: 4px;
            display: none;
        }

        .step-error {
            margin-bottom: 15px;
            font-weight: 400;
            padding: 10px;
            background: rgba(239, 68, 68, 0.1);
            border-radius: 4px;
            color: #ef4444;
            font-size: 14px;
            display: none;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="breadcrumb">
            <a href="@Url.Action("Index", "Calendar")">Camps</a> > <span>Edit</span>
        </div>
        <h1 class="page-title">Edit Camp</h1>
    </div>

    <div class="container">
        <div id="successMessage" class="success-message">
            Changes saved successfully!
        </div>

        <!-- Progress Bar -->
        <div class="progress-container">
            <div class="progress-steps">
                <div class="progress-line" id="progressLine"></div>
                <div class="step active" data-step="1" id="stepIndicator1">
                    <div class="step-circle">01</div>
                    <div class="step-label">Basic Information</div>
                </div>
                <div class="step" data-step="2" id="stepIndicator2">
                    <div class="step-circle">02</div>
                    <div class="step-label">Details</div>
                </div>
                <div class="step" data-step="3" id="stepIndicator3">
                    <div class="step-circle">03</div>
                    <div class="step-label">Location</div>
                </div>
                <div class="step" data-step="4" id="stepIndicator4">
                    <div class="step-circle">04</div>
                    <div class="step-label">Invited Facilities</div>
                </div>
            </div>
        </div>

        <!-- Form Start -->
        @using (Html.BeginForm("Edit", "CampList", FormMethod.Post, new { id = "campForm" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.CampID)

            <div class="form-content">
                <!-- STEP 1: Basic Info -->
                <div class="step-content active" id="step1">
                    <div class="step-error" id="step1Error"></div>

                    <!-- Active/Inactive Toggle -->
                    <div class="form-group">
                        <div class="toggle-container">
                            <span class="toggle-label">Status:</span>
                            <label class="toggle-switch">
                                @Html.CheckBoxFor(m => m.IsActive, new { id = "isActiveToggle" })
                                <span class="toggle-slider"></span>
                            </label>
                            <span class="status-badge" id="statusBadge">
                                @(Model.IsActive ? "Active" : "Inactive")
                            </span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="campName">Camp Name <span class="required">*</span></label>
                        @Html.TextBoxFor(m => m.CampName, new { @class = "form-control", id = "campName" })
                        <div class="error-message" id="campNameError">Camp name is required</div>
                    </div>

                    <div class="form-group">
                        <label for="description">Camp Description</label>
                        @Html.TextAreaFor(m => m.CampDescription, new { @class = "form-control", id = "description", placeholder = "Enter camp description..." })
                    </div>

                    <div class="form-group form-row">
                        <div>
                            <label for="startDate">Start Date <span class="required">*</span></label>
                            @Html.TextBoxFor(m => m.StartDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date", id = "startDate" })
                            <div class="error-message" id="startDateError">Start date is required</div>
                        </div>
                        <div>
                            <label for="endDate">End Date <span class="required">*</span></label>
                            @Html.TextBoxFor(m => m.EndDate, "{0:yyyy-MM-dd}", new { @class = "form-control", type = "date", id = "endDate" })
                            <div class="error-message" id="endDateError">End date is required and must be after start date</div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="branch">Branch <span class="required">*</span></label>
                        @{
                            var branches = new List<SelectListItem>
                                        {
                                        new SelectListItem { Value = "", Text = "Select Branch", Disabled = true },
                                        new SelectListItem { Value = "الفرع الرئيسي", Text = "الفرع الرئيسي", Selected = Model.Branch == "الفرع الرئيسي" }
                                        };
                        }
                        @Html.DropDownListFor(m => m.Branch, branches, new { @class = "form-control", id = "branch" })
                        <div class="error-message" id="branchError">Please select a branch</div>
                    </div>
            </div>

            <!-- STEP 2: Details -->
            <div class="step-content" id="step2">
                <div class="step-error" id="step2Error"></div>

                <div class="form-group form-row">
                    <div>
                        <label for="capacity">Capacity</label>
                        @Html.TextBoxFor(m => m.Capacity, new { @class = "form-control", type = "number", min = "1", id = "capacity" })
                    </div>
                    <div>
                        <label for="ticketPrice">Ticket Price (VAT incl.) <span class="required">*</span></label>
                        @Html.TextBoxFor(m => m.TicketPrice, new { @class = "form-control", type = "number", step = "0.01", min = "0", id = "ticketPrice" })
                        <div class="error-message" id="ticketPriceError">Ticket price must be 0 or greater</div>
                    </div>
                </div>

                <div class="activities-section">
                    <h3>Activities</h3>
                    <div id="activitiesContainer">
                        @if (Model.Activities != null && Model.Activities.Any())
                            {
                                for (int i = 0; i < Model.Activities.Count; i++)
                                {
                                    <div class="activity-item">
                                        <div>
                                            <label>Activity</label>
                                            @Html.TextBoxFor(m => m.Activities[i].ActivityName, new { @class = "form-control", placeholder = "Enter activity name" })
                                        </div>
                                        <div>
                                            <label>Activity Description</label>
                                            @Html.TextAreaFor(m => m.Activities[i].ActivityDescription, new { @class = "form-control", placeholder = "Enter activity description" })
                                        </div>
                                        <div style="display: flex; align-items: center;">
                                            <span class="copy-icon" onclick="duplicateActivity(this)" title="Duplicate">📋</span>
                                            <button type="button" class="delete-btn" onclick="removeActivity(this)" title="Delete">🗑</button>
                                        </div>
                                    </div>
                                }
                            }
                            else
                            {
                                <div class="activity-item">
                                    <div>
                                        <label>Activity</label>
                                        <input type="text" name="Activities[0].ActivityName" class="form-control" placeholder="Enter activity name" />
                                    </div>
                                    <div>
                                        <label>Activity Description</label>
                                        <textarea name="Activities[0].ActivityDescription" class="form-control" placeholder="Enter activity description"></textarea>
                                    </div>
                                    <div style="display: flex; align-items: center;">
                                        <span class="copy-icon" onclick="duplicateActivity(this)" title="Duplicate">📋</span>
                                        <button type="button" class="delete-btn" onclick="removeActivity(this)" title="Delete">🗑</button>
                                    </div>
                                </div>
                            }
                        </div>
                        <button type="button" class="add-btn" onclick="addActivity()">Add to activities</button>
                    </div>
                </div>

                <!-- STEP 3: Location -->
                <div class="step-content" id="step3">
                    <div class="step-error" id="step3Error"></div>
                    <h3>Location Details</h3>

                    <div class="form-group form-row">
                        <div>
                            <label for="address">Address <span class="required">*</span></label>
                            @Html.TextBoxFor(m => m.Address, new { @class = "form-control", id = "address", list = "addressSuggestions", placeholder = "Start typing an address or city..." })
                            <datalist id="addressSuggestions"></datalist>
                            <div class="error-message" id="addressError">Address is required</div>
                        </div>
                        <div>
                            <label for="city">City <span class="required">*</span></label>
                            @Html.TextBoxFor(m => m.City, new { @class = "form-control", id = "city" })
                            <div class="error-message" id="cityError">City is required</div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label>Location</label>
                        <div class="map-container" id="map"></div>
                    </div>

                    <div class="form-group form-row">
                        <div>
                            <label for="latitude">Latitude</label>
                            @Html.TextBoxFor(m => m.Latitude, new { @class = "form-control", type = "number", step = "any", id = "latitude" })
                        </div>
                        <div>
                            <label for="longitude">Longitude</label>
                            @Html.TextBoxFor(m => m.Longitude, new { @class = "form-control", type = "number", step = "any", id = "longitude" })
                        </div>
                    </div>
                </div>

                <!-- STEP 4: Invited Facilities -->
                <div class="step-content" id="step4">
                    <div class="step-error" id="step4Error"></div>

                    <div class="facilities-section">
                        <h3>Invited Facilities</h3>
                        <div id="facilitiesContainer">
                            @if (Model.InvitedFacilities != null && Model.InvitedFacilities.Any())
                            {
                                for (int i = 0; i < Model.InvitedFacilities.Count; i++)
                                {
                                    <div class="facility-item">
                                        <div>
                                            <label>Invited Facility <span class="required">*</span></label>
                                            @Html.DropDownListFor(m => m.InvitedFacilities[i].FacilityName,
                                            new SelectList(new[] {
                                                                new { Value = "", Text = "Select an option" },
                                                                new { Value = "facility1", Text = "Sports Complex A" },
                                                                new { Value = "facility2", Text = "Training Center B" },
                                                                new { Value = "facility3", Text = "Academy C" },
                                                                new { Value = "facility4", Text = "Fitness Center D" },
                                                                new { Value = "facility5", Text = "Recreation Center E" }
                                                                }, "Value", "Text"),
                                                                new { @class = "form-control", required = "required" })
                                <div class="error-message" id="facilityError@(i)">Please select a facility</div>
                            </div>
                            <div>
                                <label>Invitation Message</label>
                                @Html.TextAreaFor(m => m.InvitedFacilities[i].InvitationMessage, new { @class = "form-control", placeholder = "Enter invitation message..." })
                            </div>
                            <div style="display: flex; align-items: center;">
                                <span class="copy-icon" onclick="duplicateFacility(this)" title="Duplicate">📋</span>
                                <button type="button" class="delete-btn" onclick="removeFacility(this)" title="Delete">🗑</button>
                            </div>
                        </div>
                                                }
                        }
                                                else
                        {
                            <div class="facility-item">
                                <div>
                                    <label>Invited Facility <span class="required">*</span></label>
                                    <select name="InvitedFacilities[0].FacilityName" class="form-control" required>
                                        <option value="">Select an option</option>
                                        <option value="facility1">Sports Complex A</option>
                                        <option value="facility2">Training Center B</option>
                                        <option value="facility3">Academy C</option>
                                        <option value="facility4">Fitness Center D</option>
                                        <option value="facility5">Recreation Center E</option>
                                    </select>
                                    <div class="error-message" id="facilityError0">Please select a facility</div>
                                </div>
                                <div>
                                    <label>Invitation Message</label>
                                    <textarea name="InvitedFacilities[0].InvitationMessage" class="form-control" placeholder="Enter invitation message..."></textarea>
                                </div>
                                <div style="display: flex; align-items: center;">
                                    <span class="copy-icon" onclick="duplicateFacility(this)" title="Duplicate">📋</span>
                                    <button type="button" class="delete-btn" onclick="removeFacility(this)" title="Delete">🗑</button>
                                </div>
                            </div>
                        }
                    </div>
                    <button type="button" class="add-btn" onclick="addFacility()">Add to invited Facilities</button>
                </div>
            </div>
        </div>

        <!-- Navigation Buttons -->
        <div class="form-navigation">
            <button type="button" class="nav-btn btn-back" id="backBtn" onclick="previousStep()">Back</button>
            <div class="action-buttons" id="navActions">
                <button type="submit" class="nav-btn btn-save-changes">Save Changes</button>
                <button type="button" class="nav-btn btn-next" id="nextBtn" onclick="nextStep()">Next</button>
            </div>
            <div class="action-buttons" id="finalActions" style="display: none;">
                <button type="button" class="nav-btn btn-cancel" onclick="cancelForm()">Cancel</button>
                <button type="submit" class="nav-btn btn-save-changes">Save Changes</button>
            </div>
        </div>
                }
    </div>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script>
        let currentStep = 1;
        let activityCount = @(Model.Activities?.Count ?? 1);
        let facilityCount = @(Model.InvitedFacilities?.Count ?? 1);
        let map = null;
        let marker = null;
        let addressSuggestionsData = [];

        document.addEventListener('DOMContentLoaded', function () {
            // Initialize
            updateProgressBar();
            updateNavigationButtons();
            updateStepStatus();

            const today = new Date().toISOString().split('T')[0];
            document.getElementById('startDate').min = today;
            document.getElementById('endDate').min = today;

            // Real-time validation listeners
            setupValidationListeners();

            // Toggle switch handler
            setupToggleSwitch();

            // Initialize map if coordinates exist
            const lat = parseFloat(document.getElementById('latitude').value);
            const lng = parseFloat(document.getElementById('longitude').value);
            if (!isNaN(lat) && !isNaN(lng) && lat !== 0 && lng !== 0) {
                // Map will be initialized when step 3 is reached
            }
        });

        // Toggle Switch Setup
        function setupToggleSwitch() {
            const toggle = document.getElementById('isActiveToggle');
            const statusBadge = document.getElementById('statusBadge');

            toggle.addEventListener('change', function() {
                if (this.checked) {
                    statusBadge.textContent = 'Active';
                    statusBadge.className = 'status-badge status-active';
                } else {
                    statusBadge.textContent = 'Inactive';
                    statusBadge.className = 'status-badge status-inactive';
                }
            });

            // Set initial state
            if (toggle.checked) {
                statusBadge.className = 'status-badge status-active';
            } else {
                statusBadge.className = 'status-badge status-inactive';
            }
        }

        // Validation functions
        function validateStep1() {
            let isValid = true;
            const stepError = document.getElementById('step1Error');
            stepError.style.display = 'none';

            const campName = document.getElementById('campName');
            const campNameError = document.getElementById('campNameError');
            if (!campName.value.trim()) {
                campName.classList.add('error');
                campNameError.style.display = 'block';
                isValid = false;
            } else {
                campName.classList.remove('error');
                campNameError.style.display = 'none';
            }

            const startDate = document.getElementById('startDate');
            const startDateError = document.getElementById('startDateError');
            if (!startDate.value) {
                startDate.classList.add('error');
                startDateError.style.display = 'block';
                isValid = false;
            } else {
                startDate.classList.remove('error');
                startDateError.style.display = 'none';
            }

            const endDate = document.getElementById('endDate');
            const endDateError = document.getElementById('endDateError');
            if (!endDate.value) {
                endDate.classList.add('error');
                endDateError.textContent = 'End date is required';
                endDateError.style.display = 'block';
                isValid = false;
            } else if (startDate.value && new Date(endDate.value) <= new Date(startDate.value)) {
                endDate.classList.add('error');
                endDateError.textContent = 'End date must be after start date';
                endDateError.style.display = 'block';
                isValid = false;
            } else {
                endDate.classList.remove('error');
                endDateError.style.display = 'none';
            }

            const branch = document.getElementById('branch');
            const branchError = document.getElementById('branchError');
            if (!branch.value) {
                branch.classList.add('error');
                branchError.style.display = 'block';
                isValid = false;
            } else {
                branch.classList.remove('error');
                branchError.style.display = 'none';
            }

            if (!isValid) {
                stepError.textContent = 'Please fill in all required fields before proceeding.';
                stepError.style.display = 'block';
                setTimeout(() => stepError.style.display = 'none', 5000);
            }

            return isValid;
        }

        function validateStep2() {
            let isValid = true;
            const stepError = document.getElementById('step2Error');
            stepError.style.display = 'none';

            const ticketPrice = document.getElementById('ticketPrice');
            const ticketPriceError = document.getElementById('ticketPriceError');
            if (!ticketPrice.value || parseFloat(ticketPrice.value) < 0) {
                ticketPrice.classList.add('error');
                ticketPriceError.style.display = 'block';
                isValid = false;
            } else {
                ticketPrice.classList.remove('error');
                ticketPriceError.style.display = 'none';
            }

            if (!isValid) {
                stepError.textContent = 'Please fill in all required fields before proceeding.';
                stepError.style.display = 'block';
                setTimeout(() => stepError.style.display = 'none', 5000);
            }

            return isValid;
        }

        function validateStep3() {
            let isValid = true;
            const stepError = document.getElementById('step3Error');
            stepError.style.display = 'none';

            const address = document.getElementById('address');
            const addressError = document.getElementById('addressError');
            if (!address.value.trim()) {
                address.classList.add('error');
                addressError.style.display = 'block';
                isValid = false;
            } else {
                address.classList.remove('error');
                addressError.style.display = 'none';
            }

            const city = document.getElementById('city');
            const cityError = document.getElementById('cityError');
            if (!city.value.trim()) {
                city.classList.add('error');
                cityError.style.display = 'block';
                isValid = false;
            } else {
                city.classList.remove('error');
                cityError.style.display = 'none';
            }

            if (!isValid) {
                stepError.textContent = 'Please fill in all required fields before proceeding.';
                stepError.style.display = 'block';
                setTimeout(() => stepError.style.display = 'none', 5000);
            }

            return isValid;
        }

        function validateStep4() {
            let isValid = true;
            const stepError = document.getElementById('step4Error');
            stepError.style.display = 'none';

            const facilitySelects = document.querySelectorAll('select[name^="InvitedFacilities"]');
            let hasValidFacility = false;

            facilitySelects.forEach((select, index) => {
                const error = document.getElementById(`facilityError${index}`);
                if (!select.value) {
                    select.classList.add('error');
                    if (error) error.style.display = 'block';
                    isValid = false;
                } else {
                    select.classList.remove('error');
                    if (error) error.style.display = 'none';
                    hasValidFacility = true;
                }
            });

            if (!hasValidFacility) {
                stepError.textContent = 'Please select at least one facility.';
                stepError.style.display = 'block';
                setTimeout(() => stepError.style.display = 'none', 5000);
                isValid = false;
            }

            return isValid;
        }

        function setupValidationListeners() {
            document.getElementById('campName').addEventListener('input', function () {
                const error = document.getElementById('campNameError');
                if (this.value.trim()) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
            });

            document.getElementById('startDate').addEventListener('input', function () {
                const error = document.getElementById('startDateError');
                const endDate = document.getElementById('endDate');
                if (this.value) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                    endDate.min = this.value;
                }
            });

            document.getElementById('endDate').addEventListener('input', function () {
                const error = document.getElementById('endDateError');
                const startDate = document.getElementById('startDate');
                if (this.value && startDate.value && new Date(this.value) > new Date(startDate.value)) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
            });

            document.getElementById('branch').addEventListener('change', function () {
                const error = document.getElementById('branchError');
                if (this.value) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
            });

            document.getElementById('ticketPrice').addEventListener('input', function () {
                const error = document.getElementById('ticketPriceError');
                if (this.value && parseFloat(this.value) >= 0) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
            });

            document.getElementById('address').addEventListener('input', function () {
                const error = document.getElementById('addressError');
                if (this.value.trim()) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
                fetchAddressSuggestions(this.value.trim());
            });

            document.getElementById('city').addEventListener('input', function () {
                const error = document.getElementById('cityError');
                if (this.value.trim()) {
                    this.classList.remove('error');
                    error.style.display = 'none';
                }
            });
        }

        // Map functions
        function initializeMap() {
            if (map) return;

            const lat = parseFloat(document.getElementById('latitude').value) || 21.4225;
            const lng = parseFloat(document.getElementById('longitude').value) || 39.8262;

            map = L.map('map').setView([lat, lng], 13);
            L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                attribution: '&copy; OpenStreetMap contributors'
            }).addTo(map);

            if (lat !== 21.4225 || lng !== 39.8262) {
                marker = L.marker([lat, lng]).addTo(map);
            }

            map.on('click', function (e) {
                const newLat = e.latlng.lat;
                const newLng = e.latlng.lng;

                document.getElementById('latitude').value = newLat.toFixed(6);
                document.getElementById('longitude').value = newLng.toFixed(6);

                if (marker) {
                    map.removeLayer(marker);
                }
                marker = L.marker([newLat, newLng]).addTo(map);
                map.panTo([newLat, newLng]);
            });
        }

        // Address suggestions
        async function fetchAddressSuggestions(query) {
            if (query.length < 3) return;

            try {
                const response = await fetch(`https://nominatim.openstreetmap.org/search?q=${encodeURIComponent(query)}&format=json&addressdetails=1&limit=5`);
                const results = await response.json();
                addressSuggestionsData = results;

                const suggestionsList = document.getElementById('addressSuggestions');
                suggestionsList.innerHTML = '';
                results.forEach(result => {
                    const option = document.createElement('option');
                    option.value = result.display_name;
                    suggestionsList.appendChild(option);
                });
            } catch (error) {
                console.error('Error fetching suggestions:', error);
            }
        }

        document.getElementById('address').addEventListener('change', function () {
            const result = addressSuggestionsData.find(data => data.display_name === this.value);
            if (result) {
                const lat = parseFloat(result.lat);
                const lon = parseFloat(result.lon);
                document.getElementById('latitude').value = lat.toFixed(6);
                document.getElementById('longitude').value = lon.toFixed(6);

                const city = result.address?.city || result.address?.town || result.address?.village || '';
                document.getElementById('city').value = city;

                if (marker && map) {
                    map.removeLayer(marker);
                    marker = L.marker([lat, lon]).addTo(map);
                    map.setView([lat, lon], 13);
                }
            }
        });

        // Navigation functions
        function nextStep() {
            if (validateCurrentStep()) {
                if (currentStep < 4) {
                    document.getElementById(`step${currentStep}`).classList.remove('active');
                    currentStep++;
                    document.getElementById(`step${currentStep}`).classList.add('active');
                    updateProgressBar();
                    updateNavigationButtons();
                    updateStepStatus();
                    if (currentStep === 3) {
                        initializeMap();
                        setTimeout(() => map.invalidateSize(), 200);
                    }
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                }
            }
        }

        function previousStep() {
            if (currentStep > 1) {
                document.getElementById(`step${currentStep}`).classList.remove('active');
                currentStep--;
                document.getElementById(`step${currentStep}`).classList.add('active');
                updateProgressBar();
                updateNavigationButtons();
                updateStepStatus();
                if (currentStep === 3) {
                    initializeMap();
                    setTimeout(() => map.invalidateSize(), 200);
                }
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        }

        function validateCurrentStep() {
            if (currentStep === 1) return validateStep1();
            if (currentStep === 2) return validateStep2();
            if (currentStep === 3) return validateStep3();
            if (currentStep === 4) return validateStep4();
            return false;
        }

        // Step click navigation
        document.querySelectorAll('.step').forEach(step => {
            step.addEventListener('click', function () {
                const targetStep = parseInt(this.getAttribute('data-step'));
                let canNavigate = true;

                // Validate all previous steps
                for (let i = 1; i < targetStep; i++) {
                    if (i === 1 && !validateStep1()) canNavigate = false;
                    if (i === 2 && !validateStep2()) canNavigate = false;
                    if (i === 3 && !validateStep3()) canNavigate = false;
                }

                if (canNavigate || targetStep === 1) {
                    document.getElementById(`step${currentStep}`).classList.remove('active');
                    currentStep = targetStep;
                    document.getElementById(`step${currentStep}`).classList.add('active');
                    updateProgressBar();
                    updateNavigationButtons();
                    updateStepStatus();
                    if (currentStep === 3) {
                        initializeMap();
                        setTimeout(() => map.invalidateSize(), 200);
                    }
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                }
            });
        });

        function cancelForm() {
            if (confirm("Are you sure you want to cancel? Unsaved changes will be lost.")) {
                window.location.href = '@Url.Action("Index", "CampList")';
            }
        }

        function updateProgressBar() {
            const progressLine = document.getElementById('progressLine');
            const percentage = ((currentStep - 1) / 3) * 100;
            progressLine.style.width = `${percentage}%`;
        }

        function updateNavigationButtons() {
            const backBtn = document.getElementById('backBtn');
            const navActions = document.getElementById('navActions');
            const finalActions = document.getElementById('finalActions');

            backBtn.style.opacity = currentStep === 1 ? '0' : '1';
            backBtn.style.pointerEvents = currentStep === 1 ? 'none' : 'auto';

            if (currentStep === 4) {
                navActions.style.display = 'none';
                finalActions.style.display = 'flex';
            } else {
                navActions.style.display = 'flex';
                finalActions.style.display = 'none';
            }
        }

        function updateStepStatus() {
            document.querySelectorAll('.step').forEach((step, index) => {
                step.classList.remove('active', 'completed');
                const stepNumber = index + 1;
                const circle = step.querySelector('.step-circle');

                if (stepNumber < currentStep) {
                    step.classList.add('completed');
                    circle.innerHTML = '✔';
                } else if (stepNumber === currentStep) {
                    step.classList.add('active');
                    circle.innerHTML = String(stepNumber).padStart(2, '0');
                } else {
                    circle.innerHTML = String(stepNumber).padStart(2, '0');
                }
            });
        }

        // Activity functions
        function addActivity() {
            const container = document.getElementById('activitiesContainer');
            const activityIndex = activityCount++;
            const activityDiv = document.createElement('div');
            activityDiv.className = 'activity-item';
            activityDiv.innerHTML = `
                <div>
                    <label>Activity</label>
                    <input type="text" name="Activities[${activityIndex}].ActivityName" class="form-control" placeholder="Enter activity name" />
                </div>
                <div>
                    <label>Activity Description</label>
                    <textarea name="Activities[${activityIndex}].ActivityDescription" class="form-control" placeholder="Enter activity description"></textarea>
                </div>
                <div style="display: flex; align-items: center;">
                    <span class="copy-icon" onclick="duplicateActivity(this)" title="Duplicate">📋</span>
                    <button type="button" class="delete-btn" onclick="removeActivity(this)" title="Delete">🗑</button>
                </div>
            `;
            container.appendChild(activityDiv);
        }

        function duplicateActivity(el) {
            const parent = el.closest('.activity-item');
            const clone = parent.cloneNode(true);
            const newIndex = activityCount++;
            clone.querySelectorAll('input, textarea').forEach(input => {
                if (input.name && input.name.includes('Activities')) {
                    input.name = input.name.replace(/\[\d+\]/, `[${newIndex}]`);
                }
            });
            parent.after(clone);
        }

        function removeActivity(el) {
            const parent = el.closest('.activity-item');
            const container = document.getElementById('activitiesContainer');
            if (container.children.length > 1) {
                parent.remove();
            } else {
                alert('At least one activity must remain.');
            }
        }

        // Facility functions
        function addFacility() {
            const container = document.getElementById('facilitiesContainer');
            const index = facilityCount++;
            const facilityDiv = document.createElement('div');
            facilityDiv.className = 'facility-item';
            facilityDiv.innerHTML = `
                <div>
                    <label>Invited Facility <span class="required">*</span></label>
                    <select name="InvitedFacilities[${index}].FacilityName" class="form-control" required>
                        <option value="">Select an option</option>
                        <option value="facility1">Sports Complex A</option>
                        <option value="facility2">Training Center B</option>
                        <option value="facility3">Academy C</option>
                        <option value="facility4">Fitness Center D</option>
                        <option value="facility5">Recreation Center E</option>
                    </select>
                    <div class="error-message" id="facilityError${index}">Please select a facility</div>
                </div>
                <div>
                    <label>Invitation Message</label>
                    <textarea name="InvitedFacilities[${index}].InvitationMessage" class="form-control" placeholder="Enter invitation message..."></textarea>
                </div>
                <div style="display: flex; align-items: center;">
                    <span class="copy-icon" onclick="duplicateFacility(this)" title="Duplicate">📋</span>
                    <button type="button" class="delete-btn" onclick="removeFacility(this)" title="Delete">🗑</button>
                </div>
            `;
            container.appendChild(facilityDiv);
        }

        function duplicateFacility(el) {
            const parent = el.closest('.facility-item');
            const clone = parent.cloneNode(true);
            const newIndex = facilityCount++;
            clone.querySelectorAll('select, textarea').forEach(input => {
                if (input.name && input.name.includes('InvitedFacilities')) {
                    input.name = input.name.replace(/\[\d+\]/, `[${newIndex}]`);
                    if (input.tagName === 'SELECT') input.selectedIndex = 0;
                    else input.value = '';
                }
            });
            clone.querySelector('.error-message').id = `facilityError${newIndex}`;
            parent.after(clone);
        }

        function removeFacility(el) {
            const parent = el.closest('.facility-item');
            const container = document.getElementById('facilitiesContainer');
            if (container.children.length > 1) {
                parent.remove();
            } else {
                alert('At least one facility must remain.');
            }
        }
    </script>
</body>
</html>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}